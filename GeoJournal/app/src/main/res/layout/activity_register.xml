<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".RegisterActivity"
    android:background="#FAFAFA"> <!-- Softer background for contrast -->

    <!-- CANCEL + HEADER CONTAINER -->
    <LinearLayout
        android:id="@+id/headerContainer"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- HEADER -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="24dp"
            android:paddingBottom="24dp">

            <!-- Title -->
            <TextView
                android:id="@+id/tvSignUp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/SignUpTxt"
                android:textColor="#1A1A1A"
                android:textSize="32sp"
                android:textStyle="bold"
                android:layout_centerHorizontal="true" />

            <!-- Photo Preview -->
            <FrameLayout
                android:id="@+id/photoPreviewContainer"
                android:layout_width="140dp"
                android:layout_height="140dp"
                android:layout_below="@id/tvSignUp"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="20dp"
                android:background="@drawable/circle_white_shadow"
                android:padding="4dp">

                <ImageView
                    android:id="@+id/photoPreview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:adjustViewBounds="true"
                    android:scaleType="centerCrop"
                    android:contentDescription="Selected photo preview"
                    android:background="@drawable/rounded_form_card" />
            </FrameLayout>

            <!-- Camera button floating -->
            <ImageButton
                android:id="@+id/ivCamera"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/circle_white_shadow"
                android:padding="10dp"
                android:src="@android:drawable/ic_menu_camera"
                android:scaleType="centerInside"
                android:contentDescription="camera icon"
                android:layout_alignEnd="@id/photoPreviewContainer"
                android:layout_alignBottom="@id/photoPreviewContainer"
                android:layout_marginEnd="-8dp"
                android:layout_marginBottom="-8dp"
                app:tint="#2A2A2A" />

            <ImageButton
                android:id="@+id/btnCancel"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_alignParentEnd="true"
                android:background="@android:color/transparent"
                android:contentDescription="Cancel"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                app:tint="#2A2A2A" />
        </RelativeLayout>

        <!-- FORM CONTAINER -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/rounded_form_card"
            android:padding="24dp"
            android:elevation="4dp">

            <!-- PASSWORD -->
            <EditText
                android:id="@+id/passText"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/rounded_input"
                android:hint="@string/passTxt_hint"
                android:textColor="#1A1A1A"
                android:textColorHint="#888888"
                android:paddingLeft="18dp" />

            <!-- NAME + LASTNAME -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="16dp">

                <EditText
                    android:id="@+id/nameText"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1"
                    android:background="@drawable/rounded_input"
                    android:hint="@string/nameTxt_hint"
                    android:textColor="#1A1A1A"
                    android:textColorHint="#888888"
                    android:paddingLeft="18dp" />

                <Space
                    android:layout_width="8dp"
                    android:layout_height="1dp" />

                <EditText
                    android:id="@+id/lnameText"
                    android:layout_width="0dp"
                    android:layout_height="48dp"
                    android:layout_weight="1.3"
                    android:background="@drawable/rounded_input"
                    android:hint="@string/lnameTxt_hint"
                    android:textColor="#1A1A1A"
                    android:textColorHint="#888888"
                    android:paddingLeft="18dp" />
            </LinearLayout>

            <!-- EMAIL -->
            <EditText
                android:id="@+id/emailText"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/rounded_input"
                android:hint="example@email.com"
                android:inputType="textEmailAddress"
                android:textColor="#1A1A1A"
                android:textColorHint="#888888"
                android:paddingLeft="18dp"
                android:layout_marginTop="16dp" />

            <!-- PHONE -->
            <EditText
                android:id="@+id/phoneText"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/rounded_input"
                android:hint="0000-0000"
                android:inputType="phone"
                android:textColorHint="#888888"
                android:textColor="#1A1A1A"
                android:paddingLeft="18dp"
                android:layout_marginTop="16dp" />

            <!-- BIRTHDATE -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/rounded_input"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:paddingStart="18dp"
                android:paddingEnd="8dp"
                android:layout_marginTop="16dp">

                <TextView
                    android:id="@+id/lbBirthdate_Person"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="MM / DD / YYYY"
                    android:textColor="#444444"
                    android:textSize="15sp" />

                <ImageButton
                    android:id="@+id/btnSelectDate_person"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@android:color/transparent"
                    android:contentDescription="Select birthdate"
                    android:src="@android:drawable/ic_menu_my_calendar"
                    app:tint="#2A2A2A" />
            </LinearLayout>

            <!-- SIGN UP BUTTON -->
            <Button
                android:id="@+id/btnSignUp"
                android:layout_width="160dp"
                android:layout_height="48dp"
                android:layout_marginTop="28dp"
                android:background="@drawable/btn_green"
                android:text="SIGN UP"
                android:textColor="#FFFFFF"
                android:textAllCaps="false" />
        </LinearLayout>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>